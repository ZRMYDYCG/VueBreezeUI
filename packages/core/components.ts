import { BreButton, BreButtonGroup, BreIcon, BreCollapse, BreCollapseItem, BreAlert } from "@breeze-ui/components";
import type { Plugin } from "vue";

export default [BreButton, BreButtonGroup,BreIcon, BreCollapse, BreCollapseItem, BreAlert] as Plugin[];